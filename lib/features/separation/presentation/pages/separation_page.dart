import 'package:flutter/material.dart';

class SeparationPage extends StatelessWidget {
  const SeparationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Separation & FnF')),
      body: const Center(child: Text('Separation & FnF')),
    );
  }
}
